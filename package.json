{
  "name": "cloudbrain",
  "version": "1.0.0",
  "description": "AI Agent powered by Cloudflare Workers & AI Gateway, controlled via Telegram",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "type-check": "tsc --noEmit",
    "test": "echo \"no tests yet\""
  },
  "dependencies": {
    "hono": "^4.0.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20231218.0",
    "typescript": "^5.3.0",
    "wrangler": "^3.26.0"
  }
}
